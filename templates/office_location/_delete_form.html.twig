<form method="post" action="{{ path('office_location_delete', {'id': office_location.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ office_location.id) }}">
    <button class="btn">Delete</button>
</form>
