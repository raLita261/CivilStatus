{% extends 'base.html.twig' %}

{% block title %}Mariage{% endblock %}

{% block body %}
    <h1>Mariage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mariage.id }}</td>
            </tr>
            <tr>
                <th>DeclarationDate</th>
                <td>{{ mariage.declarationDate ? mariage.declarationDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mariage_index') }}">back to list</a>

    <a href="{{ path('mariage_edit', {'id': mariage.id}) }}">edit</a>

    {{ include('mariage/_delete_form.html.twig') }}
{% endblock %}
