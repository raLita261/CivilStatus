<form method="post" action="{{ path('parent_user_delete', {'id': parent_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parent_user.id) }}">
    <button class="btn">Delete</button>
</form>
