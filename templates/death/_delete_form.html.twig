<form method="post" action="{{ path('death_delete', {'id': death.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ death.id) }}">
    <button class="btn">Delete</button>
</form>
